# Claude Code local files
CLAUDE.md
.claude/

# Editor / local config
.vscode/

# Node / Next.js
node_modules/
.next/
npm-debug.log*
*.log

# Env files (the .example variants stay tracked)
.env
.env.local
.env*.local

# Go build artifacts
backend/spectra-rag-backend
backend/embeddump
*.exe

# C++ build output
pca_engine/build/

# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store
Thumbs.db
tsconfig.tsbuildinfo

# Eval harness: ignore the local response cache and the large raw JSON dumps,
# but commit the human-readable markdown reports as a published result artifact.
eval_cache.json
**/eval_cache.json
data/eval_results.json
quality_judge_cache.json
**/quality_judge_cache.json
data/quality_eval_results.json
data/routing_results.json
# Intermediate embedding dump (duplicate of data/routing_embeddings.json, which is committed)
data/routing_emb_classification.json
